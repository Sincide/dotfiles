# Startup Configuration
# Migrated from your existing config

exec-once = ~/.config/hypr/scripts/xdg-portal-hyprland
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Restore wallpaper + theme (this will start waybar and dunst with correct themes)
exec-once = ~/dotfiles/scripts/restore-wallpaper.sh

# Other applications (order doesn't matter for theming)
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = nm-applet --indicator
exec-once = blueman-applet
exec-once = corectrl --minimize-systray
