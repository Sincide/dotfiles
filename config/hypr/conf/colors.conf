# Color Theme Configuration
# Managed by Rice Studio
# Uses official Hyprland syntax with colors in general section

general {
    # Border Colors (supports rgb, rgba, and gradients)
    col.active_border = rgba(bd93f9ff)
    col.inactive_border = rgba(44475aaa)
}

# Workspace Colors (for Waybar/Status bars)
$workspaceActive = rgba(bd93f9ff)
$workspaceInactive = rgba(282a36ff)
$accent = rgba(bd93f9ff)
